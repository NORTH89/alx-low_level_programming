#include "holberton.h"
/**
 * _abs - absolute value
 * @i: integer
 *
 * Return: return absolute valuer of a number
 */
int _abs(int i)
{
	if (i < 0)
	{
		i = i * -1;
		return (n);
	}
	else
	{
		return (i);
	}
}
